// Widget test untuk FlowNote
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FlowNote smoke test', (WidgetTester tester) async {
    // Placeholder test — Firebase membutuhkan environment yang lebih kompleks untuk testing
    expect(true, isTrue);
  });
}
